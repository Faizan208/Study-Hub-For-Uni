import placeholderImages from '@/app/lib/placeholder-images.json';

// This file now only exports filters and potentially other static data.
// The resource data (quizzes, assignments, practicals) is now fetched from Firestore.

export const practicalFilters = ["All", "DSA Lab", "COAL Lab"];
